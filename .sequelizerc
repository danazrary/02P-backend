const path = require("path");

module.exports = {
  config: path.resolve("config", "config.cjs"),
  "models-path": path.resolve("database"),
  "seeders-path": path.resolve("seed"),
  "migrations-path": path.resolve("database", "migrations"),
};
